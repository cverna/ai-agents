apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: communishift-coreos-agent

resources:
  # secret.yaml is gitignored - copy from secret.example.yaml and fill in real credentials
  - secret.yaml
  - cronjob.yaml
